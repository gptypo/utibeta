# Beta 1.9.7 – Simplified Hero

- Removed the extra `hero_bottom.svg` cloud layer from the home hero.
- Every module hero now displays its own icon as a large background element.
- All module hero icons use the same slow rotation.
- Reduced-motion preferences disable the rotation.
