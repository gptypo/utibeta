# Beta 2.2.1 – Studio Inspector Fix

- Direct same-origin preview inspector added as a fallback to postMessage.
- Preview elements can be selected reliably after iframe load.
- Layer tree is rebuilt directly from the preview DOM.
- Layer-list selection and preview highlighting are synchronized.
- Property panel opens immediately after selection.
