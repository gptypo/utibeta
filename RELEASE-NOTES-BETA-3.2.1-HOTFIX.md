# BETA 3.2.1 – Netlify hotfix

- Javítva: `SyntaxError: Identifier '__dirname' has already been declared`.
- A background Function saját `__dirname` deklarációja `FUNCTION_DIR` névre változott.
- Az összes Netlify `.mjs` Function szintaxisa ellenőrizve.
