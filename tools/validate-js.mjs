import fs from 'node:fs';import path from 'node:path';import process from 'node:process';import {spawnSync} from 'node:child_process';import {fileURLToPath} from 'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');const files=fs.readdirSync(path.join(root,'js')).filter(name=>name.endsWith('.js')).sort();let failed=0;for(const name of files){const rel=`js/${name}`,result=spawnSync(process.execPath,['--check',path.join(root,rel)],{encoding:'utf8'});if(result.status!==0){failed++;console.error(`✗ ${rel}`);console.error(result.stderr||result.stdout);if(process.env.GITHUB_ACTIONS)console.log(`::error file=${rel},title=JavaScript syntax::A fájl szintaktikailag hibás.`)}}console.log(`ÚTITERV – JAVASCRIPT VALIDATION\n${failed?'✗':'✓'} ${files.length-failed}/${files.length} JavaScript fájl szintaktikailag valid`);if(failed)process.exitCode=1;
